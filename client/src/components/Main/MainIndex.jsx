export default function MainIndex({ token }){
     return (
    <div>
      <h1>Welcome to the Main Page</h1>
      <p>Your token: {token}</p>
      {/* replace this with the actual main page code or delete and update the call in App.jsx to display the correct page */}
    </div>
  );
};